## Developpée par Dhouha B'CHIR

Pour lancer l'application il suffit d'exécuter les commandes suivantes

* `npm install` : pour installer les dépendances
* `npm start` pour lancer l'application (http://localhost:3000)

